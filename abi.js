document.addEventListener('DOMContentLoaded', () => {
    const ctaButton = document.querySelector('.cta');
    
    ctaButton.addEventListener('click', () => {
        alert('Welcome to the Automated Attendance Management System!');
    });
});
